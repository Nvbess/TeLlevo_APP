import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CondViajeinitPage } from './cond-viajeinit.page';

describe('CondViajeinitPage', () => {
  let component: CondViajeinitPage;
  let fixture: ComponentFixture<CondViajeinitPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CondViajeinitPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
